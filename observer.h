#ifndef OBSERVER_H
#define OBSERVER_H
#include <vector>
#include <memory>


class Observer{
	public:
	// notify method for board to tell text display to update itself
	//virtual void notify(int, int, int ) = 0;
	//virtual void notify(int, int, std::shared_ptr<Subject>, int pos) = 0;
};



#endif


